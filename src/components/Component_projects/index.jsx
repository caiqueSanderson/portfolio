import styles from './Projects.module.css';

export default function Projects({ title, image, description, urlDeploy, urlGithub, }) {
    return (
        <div>
            <h2></h2>
        </div>
    )
}